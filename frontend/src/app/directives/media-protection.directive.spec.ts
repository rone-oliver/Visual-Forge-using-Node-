import { MediaProtectionDirective } from './media-protection.directive';

describe('MediaProtectionDirective', () => {
  it('should create an instance', () => {
    const directive = new MediaProtectionDirective();
    expect(directive).toBeTruthy();
  });
});
